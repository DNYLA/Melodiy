services:
  web:
    image: ghcr.io/dnyla/melodiy-web:master
    ports:
      - 4200:80
    volumes:
      - ./client/.env.production:/app/.env
  server:
    image: ghcr.io/dnyla/melodiy-server:master
    ports:
      - 5129:5129
    volumes:
      - ./server/Melodiy.Web/appsettings.production.json:/app/appsettings.json
